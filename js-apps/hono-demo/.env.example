# Database connection (unified across all environments)
# Local:   app_user with password 'dev'
# Preview: app_user with password from AWS SSM
# Prod:    app_user with password from AWS SSM
DATABASE_URL=postgresql://app_user:dev@localhost:5432/hono_demo

# Redis connection
REDIS_URL=redis://default:xxxxxxxx@localhost:6379
