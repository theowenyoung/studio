# =====================================
# Caddy 环境变量配置示例
# 本地开发：通常无需环境变量（硬编码域名）
# 生产环境：域名和邮箱从 AWS Parameter Store 获取
# =====================================

# Let's Encrypt 管理员邮箱（生产环境必需）
ADMIN_EMAIL=admin@yourdomain.com

# 主域名（生产环境必需）
TEST_DEMO_DOMAIN=yourdomain.com

# 本地开发通常不需要这些变量，使用硬编码的 app.local 等域名
