# ========================================
# 数据库连接（必填）
# ========================================

# PostgreSQL 连接（备份所有数据库）
# 格式: postgresql://user:password@host:port
# 不需要指定数据库名，使用 pg_dumpall 备份所有数据库
POSTGRES_ADMIN_URL=postgresql://postgres:your_password@postgres:5432

# Redis 连接
# 格式: redis://:password@host:port 或 redis://host:port（无密码）
REDIS_DOCKER_URL=redis://:your_password@redis:6379

# ========================================
# S3 配置（可选，用于远程备份）
# ========================================

# AWS 凭证
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# S3 存储桶名称
S3_BUCKET=

# 自定义 S3 端点（可选，用于 MinIO 等 S3 兼容服务）
# 示例: https://s3.example.com
S3_ENDPOINT=

# 环境名称（用于 S3 路径前缀区分）
# 示例: dev, staging, prod
# S3 路径格式: s3://bucket/{ENVIRONMENT}/postgres/...
ENVIRONMENT=dev
