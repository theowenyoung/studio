# Database Tasks 环境变量模板
# 部署时从 AWS Parameter Store 获取实际值

# PostgreSQL 管理员密码（从 AWS Parameter Store 获取）
POSTGRES_ADMIN_PASSWORD={{ postgres_password }}

# 应用用户密码（从 AWS Parameter Store 获取）
TEST_DEMO_PASSWORD={{ test_demo_password }}

# 任务执行配置
TASK_LOG_LEVEL=INFO
TASK_TIMEOUT=300

